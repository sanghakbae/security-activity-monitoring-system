rules_version = '2';

// Evidence uploads: any authenticated user may read/write under evidence/.
service firebase.storage {
  match /b/{bucket}/o {
    match /evidence/{recordId}/{fileName=**} {
      allow read, write: if request.auth != null;
    }
  }
}
